# URLSession Executor — Implementation Task List

**Status:** Planning. Analysis (`URLSESSION_REPORT.md`) and the highest-risk validation (mTLS from raw bytes, `HOW_TO_USE_CERTIFICATE_URLSESSION.md`) are both done. Phases 1–6 (all seven of Phase 5's sub-phases, 5a–5g) and Phase 7a are implemented; Phase 7b and everything after are not. Note on 7: Phase 7 turned out to bundle two different kinds of work once it was actually being scoped, so it split into 7a (public API surface: `preferredExecutor`/`requiredExecutor`, the `enableNetworkFramework` decision, error naming -- done) and 7b (making those modifiers actually change which client a real request executes over). 7a's own acceptance section states the caveat plainly: `requiredExecutor(_:)` genuinely validates and throws today, but pinning or preferring `.urlSession` on a compatible configuration does not yet make a real `DataTask` run over it -- that gap is exactly what 7b closes. 7b itself got the same treatment on a second pass: an initial single-block scoping of it was reviewed and found to bundle a behavior-preserving refactor, a genuinely new client capability, the live wiring, and a testing-infrastructure gap into one undifferentiated phase, so it further split into 7b1 (backend-agnostic execution abstraction, NIO-only, behavior-preserving refactor — done) → 7b2 (`Internals.URLSessionClient` becomes a real second conformance, including the combined upload/download streaming overload and the cache tee — done) → 7b3 (wire it into `RawTask`, real end-to-end traffic) → 7b4 (the two testing gaps that surfaced while scoping the above: the streamed-upload hang now that a real `UploadTask` caller reaches that path, and cancellation validated for real inside the live pipeline). 7b3 is done -- `RawTask.result()` now genuinely dispatches through `resolveExecutor()`'s decision, closing the exact gap 7a's acceptance section flagged (see 7b3 below for the two real bugs its own testing caught and fixed along the way: `requiredExecutor` not actually influencing dispatch, and GET-with-a-body failing outright under `.urlSession`). **7b3.5 (added the same day, not originally planned) is also done** -- 7b3's own unification turned out to only cover the `.urlSession`-vs-not axis, leaving `preferredExecutor(.nioTransportServices)` with zero live effect exactly the way `requiredExecutor` did before 7b3; closing that second gap meant finding and fixing a real backward-compatibility hazard for `enableNetworkFramework(_:)` (already-released public API) before `enableNetworkFramework(_:)` could finally, safely, be deprecated -- see 7b3.5 below. 7b4's cancellation item is not started, but its streamed-upload item is done via a workaround (see below). The streamed-upload gap turned out to be materially different from how 5f/7b2 originally described it — see `INPUT_STREAM_ANALISYS.md` (repo root) and 7b4's own note below: it is not `LocalServer`-specific (a real server, `httpbin.org`, hangs identically with the real bridge) and not HTTP/1.1-specific (two independent HTTP/2 servers hang too) — the actual variable, confirmed across four servers and three independently-written `InputStream` subclasses (plus a genuine `CFReadStream` built from raw callbacks), is "any custom `InputStream`/`CFReadStream`" vs. Foundation's own concrete `InputStream(data:)` class. Root cause still not found (every plausible callback-level mechanism was tried and ruled out), but the streamed-upload item is closed anyway: `Internals.URLSessionUploadStream` is deleted, replaced by `Internals.URLSessionUploadFile`, which sidesteps the bug entirely by never handing `URLSession` an `InputStream` at all — small bodies (under `inMemoryThreshold`, 8 MiB by default) upload from memory via `uploadTask(with:from:)`, anything larger spills to a temp file first and uploads via `uploadTask(with:fromFile:)` — see 7b4 below. 7b1 landed as the new `RequestExecutingClient` protocol (`Sources/RequestDL/Tasks/Sources/Raw Task/Raw/Models/`), which `RawTask`/`Internals.CacheControl` now depend on instead of `Internals.Client` directly, with `Internals.Client` as the only (still NIO-only) conformance -- full suite passes unmodified, confirming the refactor changed nothing observable yet. 7b2 landed `Internals.URLSessionClient`'s own conformance -- two new `SessionTask`-producing `execute` overloads (buffered-or-no request body, and a genuinely streamed one, both with streamed responses and an optional cache tee) plus a real `Internals.TaskSeed`-backed cancellation path, none of which needed any change to the existing `TaskDelegate` class; a genuine upload-stream-closing timing bug (would have hung a live progress bar against a *working* server) was found and fixed along the way, and a test written to dodge `LocalServer`'s known `uploadTask(withStreamedRequest:)` incompatibility was run, found to still hit it for an architectural reason not anticipated, and `withKnownIssue`-wrapped instead of shipped as a false pass. Note on 6: `Internals.ClientManager.resolvedClient(provider:sessionConfiguration:)` is additive, not a change to the existing `client(provider:sessionConfiguration:)`'s signature or behavior -- see Phase 6 below for why hooking `resolveExecutor()`'s decision straight into the existing method would have silently broken the live `RawTask`/`Internals.Session` pipeline on Darwin, where a great many everyday configurations already resolve to `.urlSession` today. Note on 5c: `.http` proxy support (`.basic`/`.basicRawCredentials` auth) works and is confirmed across macOS, Mac Catalyst, and iOS/tvOS/watchOS/visionOS Simulators — with one platform-dependent wrinkle (macOS/Catalyst bypass a configured proxy for `localhost` specifically; other platforms don't) documented in 5c below. `.socks`/`.bearer` stay excluded, as originally planned. Note on 5e: the TLS/mTLS mapping (identity building, trust roots, verification modes) is fully implemented; 3 of its 4 round-trip tests genuinely pass (trust roots, `.none` verification, identity-absent rejection), confirmed by actually running the suite on macOS *and* an iOS Simulator *and* Linux (Docker, `swift:6.2`), not on macOS alone. The 4th (a real client-certificate handshake) is `withKnownIssue` unconditionally — a missing-Keychain-Sharing-entitlement limitation of this SwiftPM test harness on *any* platform, not macOS-specific as a first pass assumed before the Simulator run corrected it; Phase 10 already flagged the underlying Keychain gap as open. That gap is now closed, not just documented: a throwaway Xcode project (Tuist-generated, not committed) with Keychain Sharing actually added confirmed the identity pairs successfully once the entitlement is present — `SecItemCopyMatching(identity) returned 1 identities`, run and observed directly on an iOS Simulator, not assumed from the spike's earlier validation. A second issue found along the way -- the shared test TLS fixtures failing `SecTrustEvaluateWithError` outright (excessive validity period, missing Extended Key Usage) -- was fixed rather than left as a known issue: see `.scripts/generate-test-certificates.sh` and `CertificateFixturesExpirationTests` in 5e below. Note on 5f: the upload-streaming bridge originally built here (`Internals.URLSessionUploadStream`, an `InputStream` subclass, unit-tested directly for byte-for-byte order/backpressure/cancellation with no `URLSession`/network involved) hit a confirmed CFNetwork bug driving it via `uploadTask(withStreamedRequest:)` — its `LocalServer`-backed integration test was a confirmed `withKnownIssue` on both macOS and iOS Simulator; the finding this note originally recorded here ("isolates the gap to `LocalServer` specifically, not the bridge") was corrected by a later, deeper investigation — see 7b4 below and `INPUT_STREAM_ANALISYS.md` — the gap is not `LocalServer`-specific at all, the real bridge hangs against `httpbin.org` too. **Superseded in 7b4 (2026-08-25):** `Internals.URLSessionUploadStream` is deleted; `Internals.URLSessionUploadFile` replaces it -- `uploadTask(with:from:)` for a body that fits in memory, `uploadTask(with:fromFile:)` for one that doesn't -- and its own unit tests (`InternalsURLSessionUploadFileTests`) and the integration tests both genuinely pass now — see 7b4's own note. Note on 5g: the download-streaming bridge reuses `Internals.DownloadBuffer` (the NIO backend's own `ReadingMode` re-chunker) unmodified rather than reimplementing it, and all of its tests -- including exact chunk-boundary assertions stronger than the phase's own acceptance bar -- genuinely pass on both macOS and an iOS Simulator, no known issues; this closes out Phase 5 entirely. Cross-platform build also confirmed via Docker (`swift:6.2` on Linux) — builds and runs the full test suite cleanly; verified no full-`Foundation` linkage in the compiled `RequestDL`/`RequestDLInternals` libraries themselves (symbol-level check), only in the swift-testing/XCTest test-harness binary, which is unavoidable framework-level linkage on Linux.
**How to use this doc:** each phase has a goal, concrete deliverables, its dependencies on earlier phases, and acceptance criteria. Phases 1–6 are a strict dependency chain — do them in order. Phase 5 is the largest and is itself split into sub-phases that can land as separate PRs. Phases 8–10 can run in parallel with anything after phase 5 lands.
**Landed already:** [#289](https://github.com/request-dl/request-dl-nio/pull/289) (merged) — the `isCompatibleWithNetworkFramework` crash/silent-drop fix and the Apple-platform `decompression` default. Both are prerequisites this plan builds on, not part of it.

---

## Phase 1 — Foundation types

**Goal:** the vocabulary every later phase shares. No behavior yet.

- [x] `Internals.ExecutorIncompatibilityReason` (enum) — one case per config field that can keep a session off a given executor. Source: `URLSESSION_REPORT.md` §6.5.
- [x] `Internals.IncompatibleExecutorConfigurationError` (struct: `requiredExecutor: Executor`, `reasons: [ExecutorIncompatibilityReason]`).
- [x] `Internals.Executor` (enum: `.urlSession`, `.nioTransportServices`, `.nio`). Source: §6.2.

**Dependencies:** none.
**Acceptance:** compiles, unit tests for `Equatable`/`Hashable` conformance where relevant. No wiring into existing code yet.

---

## Phase 2 — Capability checks (reasons, not just bools)

**Goal:** one source of truth both the soft (`preferredExecutor`) and hard (`requiredExecutor`) paths read from, replacing the standalone `Bool` check shipped in #289.

- [x] `Internals.SecureConnection.networkFrameworkIncompatibilityReasons() -> [ExecutorIncompatibilityReason]` — same fields #289 already excludes (`certificateChain`, `privateKey`, `keyLogger`, `.noHostnameVerification`, `cipherSuites`, `cipherSuiteValues`, `additionalTrustRoots`, `renegotiationSupport`, `signingSignatureAlgorithms`, `verifySignatureAlgorithms`, `sendCANameList`, `shutdownTimeout`, `pskHint`, `pskIdentityResolver`), rewritten to build a reason list instead of `&&`-chaining. Source: §6.5.
- [x] Refactor `isCompatibleWithNetworkFramework` to `networkFrameworkIncompatibilityReasons().isEmpty`. **Must not change behavior** — #289's regression tests should pass unmodified against the refactored version; that's the acceptance bar for this step.
- [x] `Internals.SecureConnection.urlSessionIncompatibilityReasons() -> [ExecutorIncompatibilityReason]` — deliberately does **not** exclude `certificateChain`/`privateKey` (validated reachable, §5.1 of the report). Excludes: `signingSignatureAlgorithms`, `verifySignatureAlgorithms`, `sendCANameList`, `renegotiationSupport`, `shutdownTimeout`, `pskHint`, `pskIdentityResolver`, `keyLogger`, `cipherSuites`, `cipherSuiteValues`. Source: §6.5.
- [x] `Internals.Session.Configuration.isCompatibleWithURLSession` (or reasons-returning equivalent) — adds the non-`SecureConnection` bucket-D checks: `dnsOverride`, `httpVersion == .http1Only`, `proxy.connectHeaders`, `proxy.connectionProtocol == .socks`. Source: §6.2.

**Dependencies:** Phase 1.
**Acceptance:** `InternalsSecureConnectionTests`'s existing NIOTS regression suite (from #289) still passes unchanged. New parameterized tests for `urlSessionIncompatibilityReasons()` mirroring that same pattern — one case per excluded field, plus explicit tests confirming `certificateChain`/`privateKey`/`additionalTrustRoots`/`.noHostnameVerification` do **not** appear in the URLSession reason list (the §6.1 "not a strict hierarchy" property, made concrete as tests).

---

## Phase 3 — `Executor` resolution

**Goal:** formalize the decision, still with only two real executors to choose between (NIOTS/NIO) — this phase adds no new transport, just the decision logic and the hard-pin error path.

- [x] `Internals.Session.Configuration.resolveExecutor() -> Executor` — tries `.urlSession` first on Darwin (reasons empty), then `.nioTransportServices`, else `.nio`. Source: §6.3. Since `Internals.URLSessionClient` doesn't exist until Phase 5, this can only ever resolve to `.urlSession` in tests as a pure function — no actual client gets built for it yet. Fine: this phase is testing the *decision*, not the execution.
- [x] `Internals.Session.Configuration.requireExecutor(_:) throws` — throws `IncompatibleExecutorConfigurationError` with the full reason list when the pinned executor can't take the config. **Must throw, never silently degrade** — this is the direct fix for the bug class #289 closed (§6.4). Source: §6.4, §6.5.
- [x] Public rewrap: `ExecutorRequirementError` (public, `RequestDL` module) following the `SecureFileLoadError` → `SecureFileError` pattern (§6.5) — descriptive, names the fix not just the failure.

**Dependencies:** Phase 2.
**Acceptance:** unit tests for `resolveExecutor()`'s priority order and for `requireExecutor(_:)` throwing with the exact expected reason set for representative configs (one test per bucket-D field is overkill here since Phase 2 already covered that per-field; this phase's tests are about the *decision*, e.g. "config compatible with both NIOTS and URLSession resolves to `.urlSession`", "config with `additionalTrustRoots` resolves to `.nio`, not `.nioTransportServices`").

---

## Phase 4 — Hoist `maximumConcurrentConnections`

**Goal:** get the concurrency cap out of `Internals.Client` (NIO-specific) before a second concrete client exists, so it isn't duplicated or allowed to drift. Source: §5.2.

- [x] Extract `connectionSemaphore` gating from `Internals.Client` into a shared wrapper (`Internals.ThrottledExecutor` or similar) that any concrete client conforms to/is wrapped by.
- [x] `Internals.Client` (NIO) adopts the wrapper; behavior must be identical to today (same tests pass unmodified).

**Dependencies:** none functionally, but do this *before* Phase 5 starts touching client structure — retrofitting it after `Internals.URLSessionClient` exists means fixing the cap twice.
**Acceptance:** existing `maximumConcurrentConnections` tests pass unmodified against the refactored structure.

---

## Phase 5 — `Internals.URLSessionClient`

**Goal:** the actual second executor. Largest phase; land as separate PRs per sub-phase rather than one giant change.

### 5a. Basic request/response

- [x] Build `URLRequest` from RequestDL's internal request representation (method, headers, URL).
- [x] Execute via `URLSession`, map `HTTPURLResponse` back to RequestDL's response head type.
- [x] Non-streaming body only at this stage (`Data` in, `Data` out) — defer 5f/5g's streaming bridges.

**Acceptance:** a `DataTask`-equivalent round-trip against `LocalServer` (no TLS customization, no redirect, no proxy) passes through `.urlSession` when forced via `requireExecutor(.urlSession)` (Phase 3). ✅ `RequestConfigurationURLSessionClientTests` (`RequestDLTests`).

### 5b. Redirects

- [x] Implement `URLSessionTaskDelegate.urlSession(_:task:willPerformHTTPRedirection:newRequest:completionHandler:)`, tracking redirect count and cycle detection manually (no native max/allowCycles in URLSession). Source: §4.3.
- [x] Map `Internals.RedirectConfiguration.disallow`/`.follow(max:allowCycles:)` onto this.

**Acceptance:** port the existing NIO-backend redirect tests (max redirects exceeded, cycle detected, disallow) to run against `.urlSession` too — same assertions, same `LocalServer` fixtures, different executor. ⚠️ No such NIO-backend suite actually existed to port (only `Internals.RedirectConfiguration.build()` unit tests and the `Session.enableRedirect`/`disableRedirect` modifier tests did) — see `InternalsURLSessionClientRedirectTests`, whose assertions are instead derived directly from AsyncHTTPClient's vendored `RedirectState.swift`, which the new delegate ports line for line.

### 5c. Proxy

- [x] `connectionProxyDictionary` mapping for `.server` (HTTP CONNECT) with `.basic`/`.bearer` auth via the proxy authentication challenge delegate callback. Source: §4.3. Implemented: `Internals.Proxy.buildConnectionProxyDictionary()`, `Internals.URLSessionClient.TaskDelegate.proxyCredential()`. **`.basic`/`.basicRawCredentials` confirmed working end to end**, on macOS, Mac Catalyst, and iOS/tvOS/watchOS/visionOS Simulators (see acceptance below). `.bearer` stays excluded on its own architectural grounds regardless of platform — `URLCredential` has exactly two shapes (user/password, identity/certificates), neither of which can carry an arbitrary token — so `Internals.ExecutorIncompatibilityReason.proxyBearerAuthorizationUnderURLSession` keeps it out of `.urlSession`.
- [x] Confirm (don't assume) whether SOCKS is usable via `connectionProxyDictionary` on current OS versions — §4.4 flagged this as unreliable/undocumented; if it doesn't work cleanly, leave `.socks` in the bucket-D exclusion list from Phase 2 rather than shipping something half-working. Not separately re-tested (the report's own "unreliable/undocumented" framing plus the narrow value of proving it broken were judged not worth the added surface); `.socks` stays excluded (`proxySOCKSUnderURLSession`, unchanged from Phase 2).

**Acceptance:** proxy tests against a local HTTP CONNECT proxy fixture (reuse whatever the NIO backend's proxy tests already stand up), for `.urlSession`. ⚠️ No such NIO-backend suite existed to reuse (same gap as 5b) — built `LocalHTTPConnectProxy` (`RequestDLTestSupport`) instead, a real local CONNECT proxy (hand-rolled `CONNECT` parsing over raw bytes — an earlier NIOHTTP1-codec-based version had a handler-removal race that crashed on every Simulator platform, never on macOS; see the type's doc comment) with a connection-attempt counter.

**Finding, in two layers, both confirmed empirically rather than assumed (first pass of this investigation reached the wrong conclusion — see below):**
1. **The mapping itself works correctly on every Apple platform checked:** macOS, Mac Catalyst, and iOS/tvOS/watchOS/visionOS Simulators all route a request through the configured proxy for a non-loopback destination — `InternalsProxyDictionaryPlatformTests` (a `LocalServer`-independent probe using a bare `Network.framework` listener) proves this directly and passes everywhere.
2. **Every platform bypasses a configured proxy for the literal loopback IP (`127.0.0.1`), but macOS and Mac Catalyst *additionally* bypass `localhost` by name — iOS/tvOS/watchOS/visionOS Simulators do not.** Since `LocalServer`/`LocalHTTPConnectProxy` are always `localhost`, `InternalsURLSessionClientProxyTests`'s round trips genuinely pass on iOS/tvOS/watchOS/visionOS and are a platform-conditioned `withKnownIssue` (`when:` gated on `os(macOS) || targetEnvironment(macCatalyst)`) on macOS/Catalyst — not a defect in the mapping, an OS policy difference this specific fixture's hostname choice exposes.

The first pass of this investigation used only a loopback destination, saw the proxy never contacted on macOS, and concluded `connectionProxyDictionary` was broken outright — wrong, caught by testing non-loopback and loopback destinations side by side (see git history / session transcript for the correction). Left as a cautionary note in `InternalsProxyDictionaryPlatformTests`'s doc comment: an executor-compatibility finding this consequential needs a same-fact-different-angle check before it changes what `Internals.ExecutorIncompatibilityReason` excludes.

**Not tested:** real (non-Simulator) iOS/tvOS/watchOS/visionOS devices — this environment has no attached hardware, and the Simulator tooling available cannot drive one. Simulators share their host Mac's kernel network stack for the actual socket I/O, so this is a meaningfully weaker guarantee than a device would be for anything that could depend on kernel-level behavior — though Catalyst matching native macOS exactly, and every Simulator platform agreeing with each other on the `localhost` question, is reasonable (not conclusive) evidence this specific finding is a CFNetwork/URLSession policy decision rather than an artifact of the simulated environment. "AppleOS 27" (next major version) does not exist yet as of this writing (current generation is 26.5 per `xcrun simctl list runtimes`; WWDC ships the next major version around June each year) — nothing to test there today; worth re-running this exact check once a 27 SDK/simulator is available, since `connectionProxyDictionary` is legacy CFNetwork API Apple has been nudging developers off of, and its behavior seems the kind of thing more likely to shift on a major version than a point release.

### 5d. Cookies

- [x] `httpShouldSetCookies = false`, no shared `httpCookieStorage` on the `URLSessionConfiguration` — mandatory normalization, not optional. Source: §4.3's "Required normalizations". Applied unconditionally in `Internals.URLSessionClient.init`, not caller-toggleable, matching "mandatory."

**Acceptance:** a test that fires two sequential requests to a server that sets a cookie, confirms the second request does **not** carry it — proving parity with the NIO backend's no-jar behavior. ✅ `InternalsURLSessionClientCookieTests` — both requests share one `Internals.URLSessionClient` (cookie jars are session-scoped, so this only proves anything if they do), the second one's `LocalServer.HTTPHandler` echoes back whatever `Cookie` header it actually received (`HTTPResult.receivedCookieHeader`, a new optional/additive field — the only way to prove the client didn't resend it, since nothing else exposes what was actually sent on the wire). Verified the test is meaningful, not a tautology: temporarily commented out the two normalization lines, confirmed the test fails (cookie *is* resent), restored them, confirmed it passes again.

### 5e. TLS / mTLS delegate

This is where the validated spike graduates into production code.

- [x] Promote `RawBytesIdentityBuilder` (from `Tests/RequestDLTests/URLSession Executor Spike/`) into `RequestDLInternals`, RSA/PKCS#1 only for the first cut (matches what was validated). Landed as `Internals.RawBytesIdentityBuilder`; the spike directory is deleted, its content promoted rather than duplicated. One behavior change from the spike: Keychain item labels are now a deterministic SHA-256 of the certificate's DER bytes (not a random UUID) and `SecItemAdd` treats `errSecDuplicateItem` as success rather than a failure — see the finding below on why the spike's `kSecValueRef`-based pre-delete wasn't actually reliable.
- [x] Promote `RoutingMTLSURLSessionDelegate`'s shape (routes by host, not the single-identity `MTLSURLSessionDelegate` used for the spike test) into the real delegate `Internals.URLSessionClient` installs — needed because one `URLSession` serves many hosts, each with its own `SecureConnection`. Landed as `Internals.URLSessionClient.TLSDelegate`, keyed by the request's own host. `Internals.URLSessionClient` itself still only ever resolves one `SecureConnection` (an init-time parameter, same as `redirectConfiguration`/`proxy`) — genuine multi-host routing has nothing to populate more than one map entry until Phase 6 pools a client across hosts, but the type's shape doesn't need revisiting when that lands.
- [x] `trustRoots`/`additionalTrustRoots` via `SecTrust` + `SecTrustSetAnchorCertificates` in the server-trust challenge. Source: §4.3. Landed in `Internals.URLSessionIdentityPolicy`, resolving both through `Internals.CertificateChain`/`TrustRoots`/`AdditionalTrustRoots`'s existing NIOSSL-backed parsing (file/bytes, PEM/DER all handled uniformly) rather than reimplementing PEM parsing.
- [x] `certificateVerification`: `.none` (accept any trust), `.fullVerification` (default), `.noHostnameVerification` (`SecPolicyCreateSSL(server: true, hostname: nil)`). Source: §4.3. All three implemented; `.none` and `.fullVerification` confirmed working end to end (see acceptance below). `.noHostnameVerification` implemented identically to the report's spec but has no dedicated round-trip test — nothing in the fixtures exercises a hostname/certificate mismatch that would tell `.fullVerification` and `.noHostnameVerification` apart.
- [x] Runtime error path for a missing Keychain Sharing entitlement (`SecItemAdd` returning `errSecMissingEntitlement`/`-34018`) — wrap it with the same actionable framing `HOW_TO_USE_CERTIFICATE_URLSESSION.md` already documents by hand, don't let a raw `OSStatus` reach the caller. Source: §5.1's "Design implication", §6.5's open item. Landed as `Internals.RawBytesIdentityBuilder.Error.missingKeychainSharingEntitlement`, thrown instead of the generic `.keychainOperationFailed` specifically for `errSecMissingEntitlement`.
- [x] Identity lifecycle: build once per `SecureConnection`, cache for reuse (mirrors NIOSSL's own per-connection TLS config caching in `Internals.ClientManager`), clean up from the Keychain when no longer needed — not on every single request. Landed as `Internals.URLSessionIdentityPolicy`, built once in `Internals.URLSessionClient.init` (not per-`execute()` call) and removed from the Keychain in `deinit`.

**Acceptance:** port `dataTask_whenCAEnabled` (the existing NIO-backend mTLS test) to run against `.urlSession`, same `LocalServer` + `Certificates()` fixtures, using `Certificate`/`PrivateKey` from `.bytes`/`.file` sources as today. Also: a test confirming `trustRoots`/`additionalTrustRoots`/`certificateVerification` variants behave the same across both executors. Ported as `RequestConfigurationURLSessionClientMTLSTests` (`RequestDLTests`) — 3 of its 4 tests genuinely complete a real handshake and pass; 1 is a confirmed, documented `withKnownIssue`.

**Cross-platform validation for this phase was done directly, not assumed** — actually run, not just reasoned about: macOS (`swift test`), an iOS 26.5 Simulator (`xcodebuild test -scheme request-dl`, SwiftPM's own auto-generated scheme, no custom entitlements), and Linux (`docker run swift:6.2 swift test`, the same image CI uses). Results:

- macOS and iOS Simulator: identical outcome, 3/4 pass for real, 1 known issue with the exact same underlying error on both (see finding 1 below) — this cross-check is what caught finding 1 being mis-scoped as macOS-specific in an earlier pass (see below).
- Linux: `Internals.URLSessionClient` and everything under `Sources/RequestDLInternals/.../URLSession Client/` is `canImport(Darwin)`-gated, so `RequestConfigurationURLSessionClientMTLSTests` compiles to nothing there by design — only `CertificateFixturesExpirationTests` (fixture-only, no TLS handshake) runs, and passes. Full suite run on Linux too (1117 tests, 0 known issues — the `withKnownIssue`s are all Darwin-only tests) to confirm the certificate regeneration below didn't break anything else there.

1. **This SwiftPM test harness has no Keychain Sharing entitlement, on any platform** (`urlSessionClient_whenMTLSConfigured_completesHandshakeMatchingNIOBackend`) — `SecItemAdd` succeeds for the certificate and the private key individually, but `SecItemCopyMatching(kSecClassIdentity)` then finds zero identities. **First pass at this got the scope wrong**: gated `withKnownIssue(when:)` on `os(macOS)`, reasoning that `swift test`'s unsigned CLI binary was the specific problem and iOS Simulator/device builds would carry the entitlement — plausible, but never actually checked, and wrong. Running the same test on an iOS Simulator produced the identical error (`Internals.RawBytesIdentityBuilder.Error.missingKeychainSharingEntitlement`'s exact message) with `when: { false }` on that platform, so it failed the suite outright instead of being recognized as the known issue it still was. The real common thread isn't the OS, it's that neither run carries the entitlement: `swift test` is a bare unsigned binary, and SwiftPM's auto-generated Xcode scheme has no `.entitlements` file at all — there's nowhere in a `Package.swift`-only project to add the Keychain Sharing capability `HOW_TO_USE_CERTIFICATE_URLSESSION.md` walks through (that doc is written for a real app target's Signing & Capabilities tab). Fixed by dropping the `when:` condition entirely — this is a property of the test harness, not the platform, so it applies unconditionally. Confirms, incidentally, that `missingKeychainSharingEntitlement`'s actionable message (5e's own runtime-error-path item) reads correctly on both platforms, since it's what both failures actually show.

   **Closed out, not left open:** the round trip *with* the entitlement present was then actually confirmed, not just assumed to work. A throwaway Xcode project (generated with Tuist, not committed) reissued `Internals.RawBytesIdentityBuilder.makeIdentity`'s exact Security-framework sequence standalone (its own code is `package`-visible, not `public`, so a separate project can't call it directly) against the same client certificate fixture bytes, in a real app target with Keychain Sharing added via Signing & Capabilities. Built and run via `xcodebuild`/`simctl` on the same iOS Simulator: `SecItemCopyMatching(identity) returned 1 identities` — `PASS -- identity paired successfully with Keychain Sharing entitlement present`. Notably, an ad-hoc `codesign --entitlements` on a hand-assembled `.app` bundle (no Xcode project) is *not* sufficient for this — Simulator launch was denied (`NSPOSIXErrorDomain` 163) until the app went through Xcode's own build system, which additionally runs a `RegisterExecutionPolicyException` build step ad-hoc signing alone doesn't get. This is the confirmation this suite structurally cannot produce itself; not repeated as an automated test since it needs a real Xcode app target with an entitlements file, not just a `Package.swift`.

**Fixed along the way, not just found:** the first pass at this acceptance test also hit `LocalServer`'s fixture server certificate failing `SecTrustEvaluateWithError` outright (even once explicitly anchored): `"'localhost' certificate is not standards compliant" / "Certificate exceeds maximum temporal validity period, Extended key usage does not match certificate usage"`. The original fixtures (`Tests/RequestDLTests/Resources/{server,client,client_password}/`, recipe in the now-superseded `SSL.md`) were issued for 30 years with no Extended Key Usage or SAN — fine for NIOSSL's own validation (no equivalent requirement), rejected outright by `SecPolicyCreateSSL`'s enforcement, uniform across every Apple platform, not macOS-specific. Fixed rather than left as a known issue, since it's a one-time fixture problem, not an environment limitation:

- [`.scripts/generate-test-certificates.sh`](.scripts/generate-test-certificates.sh) reissues all three fixtures with a `serverAuth`/`clientAuth` Extended Key Usage as appropriate, a SAN (`DNS:localhost,IP:127.0.0.1`, server only), and a 397-day validity (under Apple's enforced cap). Deliberately does **not** set `keyUsage` — a first attempt that added a critical `keyUsage` without `keyCertSign` broke every *other*, NIOSSL-backed test relying on the server certificate as its own trust anchor (`TrustRoots(server.certificateURL)`, direct, no real CA chain); BoringSSL enforces a critical `keyUsage` strictly where SecTrust apparently doesn't care about it here, so adding it was scope creep the original finding never asked for.
- [`CertificateFixturesExpirationTests`](Tests/RequestDLInternalsTests/Sources/Secure%20Connection/CertificateFixturesExpirationTests.swift) (`RequestDLInternalsTests`) reads each fixture's `notValidAfter` (`NIOSSLCertificate`, portable, no `Security`-framework dependency) and calls `Issue.record(...)` with the exact command to run if any fixture has expired — since these are now intentionally short-lived, this is what turns "some TLS test fails somewhere with an opaque handshake error a year from now" into one immediate, actionable failure instead.

`urlSessionClient_whenTrustRootsConfigured_verifiesServerCertificateAgainstThem` and `.none` verification (`urlSessionClient_whenVerificationIsNone_acceptsUntrustedServerCertificate`) both now genuinely pass, confirming `Internals.URLSessionIdentityPolicy` answers a server-trust challenge under `.fullVerification` and `.none` alike. `additionalTrustRoots` shares the same anchor-then-evaluate code path as `trustRoots` in `Internals.URLSessionIdentityPolicy`, so a dedicated test for it was judged not to add coverage; not added.

### 5f. Upload streaming

- [x] Bridge `RequestBody`/`Internals.BodySequence` (push model, `EventLoopFuture` per chunk) to `uploadTask(withStreamedRequest:)` + `needNewBodyStream` (pull model, `InputStream`). Needs a bounded-buffer adapter — not a reuse of `Internals.StreamWriterSequence`. Source: §4.3. Landed as `Internals.URLSessionUploadStream` (`Sources/RequestDLInternals/.../URLSession Client/Upload/`) — a real `InputStream` subclass (the documented subclassing contract: `open`/`close`/`read(_:maxLength:)`/`hasBytesAvailable`/`getBuffer(_:length:)`, plus the `Stream` superclass primitives), backed by a nested `Buffer` type that is the actual bounded-buffer adapter. Two-way backpressure, deliberately asymmetric: the **producer** (an unstructured `Task` draining the source `AsyncSequence<ByteBuffer>`) waits for room via a `CheckedContinuation` — a real `async` suspension, costs no thread, appropriate since it runs on the cooperative pool; the **consumer** (`read(into:maxLength:)`, called synchronously by `URLSession` off any thread with no `async` context) waits for data via a `DispatchSemaphore` — `InputStream.read` is a blocking call by contract, so blocking its calling thread is exactly what it already signed up for. `Internals.URLSessionClient` gained a second `execute(request:streaming:delegate:onUploadProgress:)` overload (generic over `some AsyncSequence<ByteBuffer> & Sendable`, so it accepts `Internals.BodySequence` directly or `RequestBody` itself from the `RequestDL` module without this module depending on that one) alongside the existing buffered `execute(request:delegate:)`; `TaskDelegate` gained `URLSessionDataDelegate` conformance (response accumulation, since `uploadTask(withStreamedRequest:)` has no `.data(for:)`-style async convenience to lean on) and `needNewBodyStream`/`didSendBodyData` handling, building a *fresh* `Internals.URLSessionUploadStream` on every `needNewBodyStream` call (retries/redirects need the body again from the start). `RequestConfiguration` gained `buildURLRequestWithoutBody()` alongside the existing (now `buildURLRequest()`-only) buffering path, since `uploadTask(withStreamedRequest:)` ignores whatever `httpBody`/`httpBodyStream` a request carries.

**Acceptance:** port existing upload-progress tests to `.urlSession`; confirm chunk delivery order and completion, not exact byte-for-byte progress-event timing (§4.3 already flags timing as executor-specific). The bridge's own correctness is unit-tested directly and passes for real — `InternalsURLSessionUploadStreamTests` (`RequestDLInternalsTests`, no `URLSession`/network involved) feeds `Internals.URLSessionUploadStream` a recognizable non-repeating byte pattern and reads it back in slice sizes that don't line up with either the source chunking or `highWaterMark`, confirming byte-for-byte order and completeness; a second test forces backpressure with `highWaterMark` far below a single chunk's size and confirms delivery still completes intact; a third confirms `close()` releases a pending `read`/`push` instead of hanging. The `LocalServer`-backed integration test (`RequestConfigurationURLSessionClientUploadTests`, `RequestDLTests`) is a confirmed `withKnownIssue` on every platform checked (macOS, iOS Simulator) — not a defect in the bridge:

**Finding, confirmed via two independent probes outside this suite, not assumed:** `uploadTask(withStreamedRequest:)` on this OS build (CFNetwork/3860.700.1, i.e. very recent) automatically negotiates the IETF ["resumable uploads" draft](https://datatracker.ietf.org/doc/draft-ietf-httpbis-resumable-upload/) for *any* streamed upload — the outgoing request carries `Upload-Draft-Interop-Version`/`Upload-Complete` headers neither RequestDL nor this suite ever asked for (confirmed by inspecting what `LocalServer` actually receives). `LocalServer` (a minimal hand-rolled NIOHTTP1 test server) answers with an entirely ordinary 200 response — correctly framed, `Content-Length` matches, `HTTPRequestDecoder` reports a clean `.head`/`.body`/`.end` sequence server-side — but the draft negotiation apparently never recognizes that response as settling the exchange: it never reaches `urlSession(_:dataTask:didReceive:completionHandler:)` on the client, and the task times out.

1. **The delegate wiring pattern itself is correct**, independent of `Internals.URLSessionUploadStream` entirely: the exact same `needNewBodyStream`/`didSendBodyData`/`didReceive`/`didCompleteWithError` sequence, built from a bare `InputStream(data:)` with none of this phase's code involved, against a real external server (`https://httpbin.org/post`) completes normally end to end.
2. **The buffered upload path against this exact `LocalServer`, same payload size, same everything else, also completes normally** (well under a second) — `session.data(for:delegate:)` never triggers `needNewBodyStream` at all, and never carries the draft headers. Only the combination of `uploadTask(withStreamedRequest:)` specifically and `LocalServer` specifically hangs.

Both `RequestConfigurationURLSessionClientUploadTests` cases use a 5-second `timeoutIntervalForRequest` (rather than the default 60) so this known, already-diagnosed hang fails fast instead of costing a full minute per run. Not investigated further into an actual fix — reissuing `LocalServer` to participate in an experimental IETF draft protocol it has no other reason to support is out of scope for this phase; a real server (as the httpbin.org probe shows) needs no special handling at all, so this is expected to be a `LocalServer`-only gap in practice, not something an app developer using this executor against a real backend would hit.

**Correction, from a much deeper investigation done while scoping 7b4 (2026-08-24) — point 1 above is misleading and should not be trusted as stated:** it's true only for the bare `InputStream(data:)` probe it literally describes, which never actually exercises `Internals.URLSessionUploadStream`. Re-run with the real bridge against `https://httpbin.org/post` directly: **it also hangs and times out**, identically to `LocalServer`. "A real server needs no special handling" and "expected to be a `LocalServer`-only gap" (this section's own closing sentence) are therefore both wrong. See `INPUT_STREAM_ANALISYS.md` (repo root) for the full re-investigation: the actual variable is "custom `InputStream` subclass" vs. Apple's own concrete `InputStream(data:)` class, confirmed with three independent subclasses (including a deliberately minimal, textbook-correct one) and four different servers (`LocalServer`, a properly chunked-decoding Python HTTP/1.1 server, two independent HTTP/2 servers, and httpbin.org itself) — not HTTP/1.1 vs. HTTP/2, not loopback vs. LAN vs. internet, not `LocalServer`'s own correctness. Root cause not yet found; tracked as Phase 7b4 below, handed off to a dedicated standalone investigation (`INPUT_STREAM_ANALISYS.md`) since it needs a RequestDL-free, SwiftNIO-free reproduction to make progress on.

**Second correction, superseding the bridge itself (2026-08-25):** the standalone investigation exhausted every callback-level hypothesis (`copyProperty`/`setProperty`, `getBuffer`, a genuine `CFReadStream` built from raw callbacks in place of a Swift subclass) without finding the mechanism — see `INPUT_STREAM_ANALISYS.md`'s §4.8–§4.10. Rather than keep chasing root cause, Phase 7b4 replaced `Internals.URLSessionUploadStream` outright with `Internals.URLSessionUploadFile`, which drains the body into memory (`uploadTask(with:from:)`) when it's small enough, or spills to a temporary file first (`uploadTask(with:fromFile:)`) when it isn't — either way, a code path that never touches `InputStream`/`needNewBodyStream` at all, instead of `uploadTask(withStreamedRequest:)`. Both `RequestConfigurationURLSessionClientUploadTests` cases above now genuinely pass (no `withKnownIssue`, no short-timeout workaround needed to fail fast); see 7b4 below for the full account. This section's own description of `Internals.URLSessionUploadStream`'s design is now history, not current behavior -- that type is deleted from the codebase.

### 5g. Download streaming / progress

- [x] `URLSessionDataDelegate.urlSession(_:dataTask:didReceive:)` → feed into the existing `DownloadProgress`/`ReadingMode` pipeline. Source: §4.3. Landed as a third `Internals.URLSessionClient.execute(request:readingMode:delegate:) -> Internals.DownloadStep` overload, returning as soon as the response head arrives (mirroring `Internals.AsyncResponse`'s own shape) rather than waiting for the whole body like the buffered/streamed-upload overloads do. Reuses `Internals.DownloadBuffer` -- the exact type `Internals.Session.execute(...)` already builds for the NIO backend -- unmodified, rather than reimplementing `ReadingMode` chunking: it turned out to already be transport-agnostic (it consumes `Internals.AnyBuffer`, not a NIO `ByteBuffer`), so `didReceive data:` only has to bridge one `Data` chunk into one `Internals.DataBuffer` per call. That bridge is synchronous, not `Task`-wrapped, on purpose: `DownloadBuffer.append(_:)` enqueues onto an order-preserving queue, and building the `DataBuffer` on a detached `Task` would let two chunks race to enqueue and reassemble the body out of order -- the exact failure mode `Internals.Buffer`'s existing "Synchronous construction, in memory only" extension (`Internals.ByteURL` → sync `Internals.DataBuffer` init) was already built to prevent for `Internals.ClientResponseReceiver`'s equivalent NIO-side callback; this reuses that same extension rather than adding a new one. `TaskDelegate` resolves the response head from `didReceive response:completionHandler:` (checking `redirectError` there too, not only in `didCompleteWithError:`, since a refused redirect means this *is* the final response) and, separately, closes/fails the `DownloadBuffer` from `didCompleteWithError:` -- the throttle slot `execute` acquires is released there as well, not via a `defer` in `execute` itself, since `execute` returns well before the download finishes.

**Acceptance:** port existing `DownloadProgress`/`ReadingMode` tests to `.urlSession`; assert final assembled data and total byte counts match, not exact chunk boundaries (executor-determined, per §4.3). Ported as `RequestConfigurationURLSessionClientDownloadTests` (`RequestDLTests`) — all 3 tests genuinely pass, no known issues this time (unlike 5f, this uses `session.dataTask(with:)`, not `uploadTask(withStreamedRequest:)`, so it never triggers the resumable-uploads-draft hang that suite documents). Went past the acceptance note's minimum bar deliberately: since `Internals.DownloadBuffer`'s re-slicing guarantee doesn't depend on how its *input* happened to be fragmented, exact chunk-boundary assertions (every `.length`-mode chunk but the last is precisely the configured length; every `.separator`-mode chunk but a trailing remainder ends with the separator) were expected to hold across executors too, not just on NIO -- and they do, confirmed rather than assumed, including a `.separator` test whose payload is sized so the separator lands at executor-determined (not test-controlled) positions relative to `didReceive data:`'s own chunk boundaries. A fourth test confirms a refused redirect (Phase 5b) surfaces as a thrown error from `execute(request:readingMode:delegate:)` itself rather than a bogus successful `DownloadStep` -- the one piece of wiring specific to resolving the response head *early* that the other two `execute` overloads' `didCompleteWithError:`-only redirect check didn't already cover. Confirmed on macOS and an iOS Simulator; Linux (Docker, `swift:6.2`) compiles this whole suite to nothing, as expected -- `Internals.URLSessionClient` is `canImport(Darwin)`-gated -- full Linux suite re-run clean (1117 tests) to confirm nothing else regressed.

**This closes out Phase 5** — all seven sub-phases (5a through 5g) are now implemented.

---

## Phase 6 — Wire into `Internals.ClientManager`

**Goal:** make `resolveExecutor()`/`requireExecutor(_:)` (Phase 3) actually select and cache `Internals.URLSessionClient` (Phase 5), not just decide in the abstract.

- [x] Extend `Internals.ClientManager`'s cache keying (today keyed by `SessionProviderOptions.isCompatibleWithNetworkFramework`) to a third case for `.urlSession`. Landed as a new `package func resolvedClient(provider:sessionConfiguration:)`, additive alongside the existing `client(provider:sessionConfiguration:)` rather than replacing it -- see the note below on why. Both now share one `_table`/`Item` structure keyed off a new `Internals.ClientManager.Client` enum (`.nio(Internals.Client)` / `.urlSession(Internals.URLSessionClient)`, the latter `canImport(Darwin)`-gated), with `resolvedClient` prefixing its cache key `"URLSession." + provider.uniqueIdentifier(with:)` so a `.urlSession` entry can never collide with, or be handed back for, a `.nio`/`.nioTransportServices` one from the same provider (which keys off the bare id, or `"NTW."`-prefixed -- see `SharedSessionProvider`/`IdentifiedSessionProvider`). `_reusableClient` was generalized to `_reusableItem`, returning the shared enum so the age/reuse logic is written once, not duplicated per backend.
- [x] Apply Phase 4's hoisted concurrency-cap wrapper to the new client so `maximumConcurrentConnections` behaves identically regardless of which executor a session lands on. Already true structurally since Phase 5a (`Internals.URLSessionClient` has owned an `Internals.ThrottledExecutor` from its first cut); this phase's actual job was making sure `resolvedClient`'s new `_createNewURLSessionClient` passes `sessionConfiguration.maximumConcurrentConnections` through when building one, same as `_createNewClient` already does for `Internals.Client`.

**Not in Phase 3/5's scope, added here because `Internals.ClientManager` needed it to build a real `Internals.URLSessionClient` from a full `Internals.Session.Configuration` rather than the `.ephemeral` base every Phase 5 test constructs by hand:**

- `Internals.Session.Configuration.buildURLSessionConfiguration()` (Darwin-only, mirrors `build() -> HTTPClient.Configuration`) -- `URLSessionConfiguration.ephemeral` as the base (no disk-backed cache/cookie storage to leak across cache entries this manager pools and later shuts down), with only `timeout.read` mapped onto `timeoutIntervalForRequest` (`URLSessionConfiguration` has no distinct connect-phase timeout for `timeout.connect` to land on). Every other field either has no `URLSessionConfiguration` counterpart to translate to (`connectionPool`, `ignoreUncleanSSLShutdown`, `networkFrameworkWaitForConnectivity`) or is already excluded from resolving to `.urlSession` at all by `urlSessionIncompatibilityReasons()` (`dnsOverride`, `httpVersion == .http1Only`, `proxy.connectHeaders`/`.socks`/`.bearer`, `decompression == .disabled`), so there is nothing left for a compatible configuration to lose in translation. `compression` (request-body gzip) is a known, pre-existing gap in `urlSessionIncompatibilityReasons()` itself, not something this phase introduces or was asked to fix -- `URLSessionConfiguration` has no request-compression equivalent to `NIOHTTPRequestCompressor`'s debug-initializer hook, and bucket D never excluded it, so it silently goes unhonored under `.urlSession` today.
- `Internals.URLSessionClient.isRunning`/`shutdown()` -- `Internals.ClientManager`'s idle-cleanup sweep (`cleanupIfNeeded()`) reads `item.client.isRunning`/calls `item.client.shutdown()` on every cached entry regardless of backend, and neither existed on `Internals.URLSessionClient` before this phase (Phase 5's tests never pooled it through `ClientManager`, so nothing needed them). `isRunning` reuses `Internals.ClientOperationQueue` -- the same transport-agnostic in-flight counter `Internals.Client` already uses -- threaded through all three `execute` overloads (the download overload's operation completes from `onDownloadComplete`, alongside the throttle release, since that overload returns before the download itself finishes). `shutdown()` mirrors `Internals.Client.shutdown()`'s idempotent, not-while-running guard, then calls `session.invalidateAndCancel()`.

**Deliberately not done in this phase, left for Phase 7:** `Internals.Session.execute`/`RawTask`/`Internals.CacheControl` -- the actual per-request pipeline every public `DataTask`/`UploadTask`/`DownloadTask` goes through -- are untouched and still hardcoded to `Internals.Client`/`HTTPClient.Request` unconditionally. This is why `resolvedClient` is a new, additive method rather than a change to `client(provider:sessionConfiguration:)`'s existing signature: `resolveExecutor()` already resolves a great many everyday configurations to `.urlSession` on Darwin (it's tried first, see Phase 3), so if the *existing* method started honoring that decision, every current Apple-platform caller through `Internals.Session.client()` would immediately start getting back a client shape `RawTask`/`CacheControl` have no code path to run a request against -- silently breaking the live pipeline on the very platforms this feature targets, rather than adding to it. Reconciling `RawTask`'s NIO-only pipeline with a second, differently-shaped executor is exactly the kind of real, unresolved design question Phase 7 already flags for itself (§6.4); `resolvedClient` is the ready-made entry point for whatever that design lands on, proven independently at the `ClientManager` level in the meantime.

**Dependencies:** Phases 3, 4, 5.
**Acceptance:** an end-to-end test with no explicit executor preference, on a config compatible with all three executors, confirms `.urlSession` is actually the one that runs (not just what `resolveExecutor()` says in isolation) — e.g. by asserting on some URLSession-specific side effect, or via a debug hook. `InternalsClientManagerExecutorTests` (`RequestDLInternalsTests`): a default `Internals.Session.Configuration()` (`resolveExecutor() == .urlSession`, confirmed inline before the real assertion) resolved through `Internals.ClientManager.resolvedClient(provider:sessionConfiguration:)` unwraps to the `.urlSession` case, and the `Internals.URLSessionClient` inside it completes a real round trip against `LocalServer` -- proving `resolvedClient` built and cached an actually-usable client, not just a matching enum case. Two more tests confirm the cache actually pools (`resolvedClient` called twice with the same provider/configuration returns the identical `Internals.URLSessionClient` instance, `===`) and that the executor-aware path still falls back correctly (a SOCKS-proxy configuration -- excluded from `.urlSession` since Phase 2 -- resolves to `.nio` through `resolvedClient`, not `.urlSession`). All existing `Internals.ClientManager`/`Internals.URLSessionClient` tests, and the full suite (1205 tests), pass unmodified.

---

## Phase 7 — Public API

**Goal:** expose executor selection to callers. This is the one phase with real open design questions (§6.4) rather than a spec to implement — resolve them here, don't guess earlier.

Split into sub-phases the same way Phase 5 was, once it became clear the phase bundled two genuinely different kinds of work: a self-contained public-API surface (7a) versus reconciling the live request-execution pipeline with a second, differently-shaped executor (7b) — the "real open design questions" the goal above refers to, and explicitly *not* solved by 7a. Shipping 7a alone is deliberate, not partial: `preferredExecutor`/`requiredExecutor` are useful on their own terms even before 7b lands (see 7a's own note on what "useful" means without it) — see the discussion at the point 7a landed for the reasoning.

### 7a. Modifiers, error naming, and validation — done

- [x] `Session.preferredExecutor(_:)` — soft hint, always falls back safely (Phase 3). Landed on `Session` (`Sources/RequestDL/Properties/Sources/Session/Session/Session.swift`), setting the new `Internals.Session.Configuration.preferredExecutor: Internals.Executor?` field. `resolveExecutor()` (Phase 3) now consults it as a tiebreaker: it reorders among whatever `urlSessionIncompatibilityReasons()`/`networkFrameworkIncompatibilityReasons()` already say is compatible, and is silently ignored (falls through to the default priority order) if the preferred executor isn't one of those — never forces an incompatible one, per §6.4.
- [x] `Session.requiredExecutor(_:)` — hard pin, throws `ExecutorRequirementError` (Phase 3). Landed the same way, setting `Internals.Session.Configuration.requiredExecutor: Internals.Executor?`. Unlike `preferredExecutor`, this needed a real hook into the live request path to mean anything: `RawTask.result()` (`Sources/RequestDL/Tasks/Sources/Raw Task/Raw/RawTask.swift`) now calls `configuration.requireExecutor(_:)` immediately after `Resolve.build()` — before the logger, client, or cache control are built, let alone any network I/O — and rewraps `Internals.IncompatibleExecutorConfigurationError` into the public `ExecutorRequirementError`, following the exact `SecureFileLoadError`→`SecureFileError` precedent §6.5 points at. This is a real, load-bearing validation: a `DataTask`/`UploadTask`/`DownloadTask` pinned to an executor its configuration can't run on throws today, for real, before this PR — see the caveat below for what pinning to a *compatible* executor does and does not additionally guarantee yet.
- [x] Decide `enableNetworkFramework(_:)`'s fate: deprecate in favor of `preferredExecutor(.nioTransportServices)`, or keep both. Source: §6.5's open items. **Decision: keep both, for now — do not deprecate yet.** Deprecating it today would point callers at `preferredExecutor(_:)`, which (per the caveat below) has no effect yet on which executor a real request actually runs on; `enableNetworkFramework(_:)` is still the only modifier that actually gates NIOTransportServices in the live pipeline today (via `isCompatibleWithNetworkFramework`/`SessionProviderOptions`, which `Internals.ClientManager.client(provider:sessionConfiguration:)` — the method the live pipeline calls — still reads, unchanged since #289). Revisit this once 7b unifies both modifiers' decisions into the one path that actually selects a client; documented inline on `enableNetworkFramework(_:)` itself so this isn't just tribal knowledge.
- [x] Public naming pass for `ExecutorRequirementError.Reason`'s cases — the internal `ExecutorIncompatibilityReason` names (e.g. `.noHostnameVerificationUnderNetworkFramework`) are fine for `Internals`, not for a public error message; needs prose-quality public names/descriptions. **Judged already satisfied, not re-done:** `ExecutorRequirementError.Reason` (landed ahead of this phase, alongside Phase 3's prep work) already carries a `CustomStringConvertible` `description` per case — the actual text a caller sees in an error message — and every one of those descriptions already reads as prose (`"a client certificate chain"`, `"disabled hostname verification (unsupported under Network.framework)"`, etc.), confirmed case-by-case by `ExecutorRequirementErrorTests.reason_whenEveryInternalCaseMapped_hasNonEmptyDescription`. The enum *case names* themselves stayed structurally identical to `Internals.ExecutorIncompatibilityReason`'s (e.g. still `.dnsOverrideUnderURLSession`, not renamed to something case-name-prose-ier) — deliberately: a caller who pattern-matches on `reasons` needs the executor-specific qualifier to stay meaningful (§6.1's "not a strict hierarchy" — the same field can be a reason under one executor and not another), and renaming identifiers already covered by good `description` text for a second time was judged churn without a clear reader benefit, not a gap left unfilled.

**Caveat, stated plainly rather than left implicit:** none of the above changes which client actually executes a real request. `Internals.ClientManager.client(provider:sessionConfiguration:)` (what `Internals.Session`/`RawTask` call today) does not consult `resolveExecutor()`, `preferredExecutor`, or `requiredExecutor` at all — only `Internals.ClientManager.resolvedClient(provider:sessionConfiguration:)` (Phase 6, additive, not yet called from `RawTask`) does. So today: `requiredExecutor(_:)` genuinely validates and throws for an incompatible configuration (real, useful, tested below); `preferredExecutor(_:)` genuinely changes what `resolveExecutor()` returns as a pure function (real, tested below); but pinning or preferring `.urlSession` on a *compatible* configuration does not make a real `DataTask` actually run its request over `Internals.URLSessionClient` yet — it still runs over the NIO backend, same as before this phase. Closing that gap is 7b's entire job.

**Dependencies:** Phase 6 (there should be something real to select before the public API ships).
**Acceptance:** DocC coverage for both modifiers — added, following the existing modifier-doc style on `Session` (parameter/return docs, a usage example, cross-references between the two). A test suite specifically for `requiredExecutor`'s error messages being actionable (not just "it throws") — `DataTaskTests.dataTask_whenRequiredExecutorIsIncompatible_throwsActionableErrorBeforeAnyNetworkIO` drives the real public `DataTask` entry point (not just `Internals.Session.Configuration.requireExecutor(_:)` directly, which `InternalsSessionConfigurationExecutorTests` already covered) and asserts the thrown `ExecutorRequirementError` names the pinned executor, the exact conflicting reason, and points at `.preferredExecutor(_:)` as the escape hatch — confirmed to throw with *no* `LocalServer`/network I/O involved, since the check runs before any client is built. A paired test confirms a *compatible* `requiredExecutor` does not throw and the request completes normally. `InternalsSessionConfigurationExecutorTests` gained a matching block of `resolveExecutor()`-with-`preferredExecutor` tests (tiebreak honored when compatible, silently ignored and falls through the default order when not, `.nio` always wins since it has no reasons to conflict with). Full suite (1213 tests) passes; `swift format lint --recursive --strict` clean.

### 7b. Pipeline integration

**Goal:** make `preferredExecutor`/`requiredExecutor` (7a) actually change which client a real `DataTask`/`UploadTask`/`DownloadTask` executes over, not just which one validation checks against. This is the reconciliation work 7a's own caveat and Phase 6's note both point at — closing it is what turns `Internals.ClientManager.resolvedClient(provider:sessionConfiguration:)` (Phase 6) from a proven-in-isolation capability into something a real caller's traffic goes through.

The first attempt at scoping this treated it as one undifferentiated block and got corrected in review: it actually bundles a behavior-preserving refactor, a genuinely new capability inside `Internals.URLSessionClient`, the live wiring itself, and a testing-infrastructure gap — four things with different risk profiles that don't need to land together. Split into 7b1–7b4 below, strict dependency order, mirroring how Phase 5 became 5a–5g once it stopped being one line item. Two findings from that review are worth stating up front because they de-risk what looked like the hardest part:

- **The "upload progress + streaming download at once" gap (7b2) is smaller than it first looked.** `Internals.URLSessionClient`'s `TaskDelegate` already implements both `didSendBodyData` (feeds `onUploadProgress`) and `didReceive data:`/`didReceive response:completionHandler:` (feeds `downloadBuffer`/`headCompletion`) unconditionally — the three existing `execute` overloads just never activate both sets of optional fields on the same call. `URLSessionDataDelegate`'s callbacks fire on `uploadTask(withStreamedRequest:)` the same way they do on a plain `dataTask(with:)` (5f's own implementation note already relies on this for response-head resolution). A fourth `execute` overload that builds the task via `uploadTask(withStreamedRequest:)` (for a genuinely streamed request body) while also wiring `downloadBuffer`/`onDownloadComplete` (for a streamed response) is mostly a new call site over existing machinery, not new architecture. And for the common case — a request body already fully known before it's sent — `didSendBodyData` fires on a plain `dataTask(with:)` too, so upload progress and download streaming already coexist for free there; no new overload needed for that half at all.
- **The cache write-through tee (7b2) already has a transport-agnostic home to plug into.** `Internals.DownloadBuffer.cacheStream(_:)` — the thing that actually duplicates every dispatched chunk to both the caller-facing stream and a cache-writer stream (see its `_dispatch`) — has no NIO dependency and is already reused verbatim by `Internals.URLSessionClient` (Phase 5g). `Internals.ClientResponseReceiver.didReceiveHead` just calls `self.download.cacheStream(cache(head))` once the response head resolves. The `.urlSession` side needs the identical call at the identical point (`TaskDelegate`'s head-resolution callback) — not a redesign, the same one-line hookup NIO already does, reusing the same buffer type.

### 7b1. Backend-agnostic execution abstraction — behavior-preserving refactor, NIO only — done

**Goal:** give `RawTask`/`Internals.CacheControl` a common abstraction to run against instead of `Internals.Client` concretely, *before* a second implementation exists behind it — so this step is pure refactor, reviewable and testable in complete isolation from anything URLSession-shaped. Motivated by more than just this one addition: a protocol boundary here also means a future third backend (e.g. whatever cross-platform networking API Apple/Swift eventually standardizes) is another conformance, not another rewrite of `RawTask`/`CacheControl`.

- [x] Define a package-internal protocol capturing exactly what `RawTask.result()` and `Internals.CacheControl` need from a client today: execute a request (optionally with the cache-tee closure and a logger) and get back a `SessionTask`, plus whatever `getUpdatedHeadersForCache`'s revalidation call needs (today a bare `client.execute(request:logger:).response()` for the head). Name and exact shape TBD at implementation time — the constraint is it must be satisfiable by wrapping the *existing* `Internals.Client` API without changing that API's own signature. Landed as `RequestExecutingClient` (`Sources/RequestDL/Tasks/Sources/Raw Task/Raw/Models/RequestExecutingClient.swift`) — a plain top-level `package protocol`, not nested under `Internals`, matching `SessionProvider`'s own precedent (also package-internal, also not `Internals`-namespaced). Declared in `RequestDL`, not `RequestDLInternals`: both methods take `RequestConfiguration`, a `RequestDL`-only type, and `RequestDLInternals` can't depend back on the module that depends on it. Two methods, both operating on the already-neutral `Internals.ResponseHead`/`RequestConfiguration`/`SessionTask` types rather than anything NIO-specific: `execute(configuration:cache:logger:) -> SessionTask` (the main streaming round trip) and `revalidationHead(configuration:logger:) -> Internals.ResponseHead` (what cache revalidation needs, without paying for a full streaming `SessionTask` it would just discard).
- [x] `Internals.Client` (or a thin adapter around it, if changing `Internals.Client`'s own surface is undesirable) conforms. Landed as `extension Internals.Client: RequestExecutingClient` (`Internals.Client+RequestExecutingClient.swift`, same directory) — `Internals.Client` conforms directly, no adapter type needed. Both methods are thin: build the `HTTPClient.Request` the way `RequestConfiguration.build(eventLoop:)` already did at the old call site, then call straight through to what `Internals.Client` already did. No new client behavior.
- [x] `RawTask.result()` and `Internals.CacheControl` depend on the protocol type, not `Internals.Client` directly. Both now hold `any RequestExecutingClient` instead of `Internals.Client`. `RawTask.result()` also lost its own `resolved.requestConfiguration.build(eventLoop:)` call and the `resolved.session.execute(client:request:...)` call entirely -- both folded into `client.execute(configuration:cache:logger:)`, a net simplification at the call site, not just a type swap.
- [x] No second conformance yet. `Internals.URLSessionClient` is untouched by this sub-phase. Confirmed -- nothing under `Sources/RequestDLInternals/Sources/Client/URLSession Client/` changed.

**Found while implementing, not anticipated in the scoping discussion:** `Internals.Session.execute(client:request:url:readingMode:uploadingBytes:cache:logger:)` (`RequestDLInternals`) turned out to be the actual home of the logic the new protocol conformance needed -- and its body never touched `self` (`provider`/`configuration`/`manager`), only the `client` parameter it was handed. Rather than duplicate that body into the new `RequestDL`-side conformance (the two-implementations-that-can-drift problem this sub-phase's own motivation was written to avoid), it moved onto `Internals.Client` itself as a new `execute(request:url:readingMode:uploadingBytes:cache:logger:)` overload, and `Internals.Session.execute` became a one-line forwarder to it -- same signature, same behavior, zero duplication. This mattered concretely: `Internals.Session.execute` has three *direct* test callers outside `RawTask` entirely (`SessionExecutionTests`, `LocalServerConcurrencyTests`, `InternalsClientResponseReceiverTests`, none of which have access to `RequestConfiguration` from `RequestDLInternalsTests`), so its exact signature had to survive untouched.

**Dependencies:** 7a.
**Acceptance:** the full existing suite passes **unmodified** — same bar Phase 2 held `isCompatibleWithNetworkFramework`'s refactor to. Every currently reachable path (`.nio`, `.nioTransportServices`, caching, revalidation, cancellation) must be byte-for-byte the same as before this sub-phase, since nothing about *what* runs a request has changed yet, only what it's referenced through. Confirmed: full suite (1213 tests) passes with the same 5 known issues as before this sub-phase, zero new failures -- including every cache-revalidation test (`cache_whenReloadAndValidateCachedData*`), which exercises `getUpdatedHeadersForCache`'s rewritten body specifically, and all three of `Internals.Session.execute`'s direct callers, confirming the forwarder. `swift format lint --recursive --strict` clean.

### 7b2. `Internals.URLSessionClient` becomes a real second conformance — done

**Goal:** give `Internals.URLSessionClient` everything 7b1's protocol needs, so it's a genuine, pluggable second implementation — still not wired into `RawTask` yet (that's 7b3), so this sub-phase's own tests exercise it directly, the same way the Phase 5/6 suites already do.

- [x] New combined `execute` overload: streamed request body (`uploadTask(withStreamedRequest:)`, via the existing `makeBodyStream`) together with a streamed response (`downloadBuffer`/`headCompletion`), reusing `TaskDelegate`'s already-implemented callback methods rather than adding new ones — confirmed, not just reasoned about: `TaskDelegate` needed zero changes. Landed as two new `SessionTask`-producing methods on `Internals.URLSessionClient` (not a `DownloadStep`-returning one as first sketched — see the finding below): `execute(request:readingMode:uploadingBytes:cache:logger:delegate:)` (no streamed request body) and `execute(request:streaming:readingMode:uploadingBytes:cache:logger:delegate:)` (streamed request body too), sharing a private `executeSessionTask(...)` that differs only in `makeBodyStream`/which `URLSession` task constructor to use. Each builds its own `Internals.AsyncStream<Int>`/`Internals.AsyncStream<ResponseHead>` pair, fed by `onUploadProgress`/`headCompletion` respectively, and reuses `Internals.DownloadBuffer.stream` as the `download` stream directly — the exact three-stream shape `Internals.AsyncResponse` already expects from the NIO side.
- [x] `cache: ((Internals.ResponseHead) -> Internals.AsyncStream<Internals.DataBuffer>?)?` threaded onto the download-capable `execute` overload(s), calling `downloadBuffer.cacheStream(cache(head))` once the head resolves — landed exactly as predicted, and turned out to need **zero changes to `TaskDelegate`**: both new methods already set `taskDelegate.headCompletion` themselves (to push into the `head` stream instead of resuming a continuation), so the cache-tee call sits right there in that same closure, at the same point in the callback sequence `Internals.ClientResponseReceiver.didReceiveHead` attaches it on the NIO side.
- [x] The revalidation-request primitive `getUpdatedHeadersForCache` needs: a plain (non-streaming) `execute(request:delegate:)` call is sufficient here, no new capability required. Confirmed — `Internals.URLSessionClient+RequestExecutingClient.swift`'s `revalidationHead(configuration:logger:)` calls the existing (Phase 5a) `execute(request:delegate:)` unchanged.
- [x] Replace `HTTPClientError.cancelled` in `Internals.CacheControl.makeCachedSession`'s synthetic cancel path with a new, neutral, transport-agnostic error. Landed as `Internals.TaskCancelledError` (`RequestDLInternals`, `Sources/RequestDLInternals/Sources/Session/Session/Models/`) — an empty `Error, Sendable` struct with a doc comment naming exactly why it exists (fires on a cache *hit*, unrelated to which executor a *miss* would run on).
- [x] `Internals.URLSessionClient` (via a 7b1-conforming adapter) is now a complete, standalone second implementation of the protocol. Landed as `Internals.URLSessionClient+RequestExecutingClient.swift` (`RequestDL`, `Darwin`-gated) — a request with a body always streams it (uniform with the NIO backend's own unconditional streaming, not a size heuristic this conformance invents).

**Findings from actually building this, not anticipated when 7b was scoped:**

1. **The plan's "combined `DownloadStep`-returning overload" turned out to be the wrong shape entirely, caught by building it and hitting a type mismatch, not by re-reading the plan more carefully.** `Internals.AsyncResponse`'s `upload`/`head`/`download` need to be *live*, pushed into as events happen, for a consumer iterating `AsyncResponse` mid-transfer to see real progress — a `DownloadStep` (an already-fully-resolved head plus a bytes stream, `execute(request:readingMode:delegate:)`'s existing Phase 5g return shape) can't carry "upload progress that happened before the response arrived" at all. The two new methods build `Internals.AsyncStream`s directly instead, exactly mirroring `Internals.Client.execute(request:url:readingMode:uploadingBytes:cache:logger:)` (7b1) rather than reusing `DownloadStep` as an intermediate.
2. **Cancellation needed a real design decision the plan didn't anticipate**: none of the three original `execute` overloads (5a/5f/5g) ever hand the caller a reference to the underlying `URLSessionTask`, so nothing could `cancel()` it. The two new methods construct the task inline and capture it directly in `Internals.TaskSeed { task.cancel() }` — simpler than `Internals.UnsafeTask`'s NIO/event-loop-affinity dance, exactly as 7b4's own text predicted, and unlike NIO's split `cancel`/`release` closures, `task.cancel()` is safe to call from anywhere at any time (including on an already-completed task), so the single-closure convenience initializer is sufficient.
3. **A genuine, pre-existing-in-this-sub-phase timing bug, found while writing this, not before**: the first draft closed `upload` only from `onDownloadComplete` (whole-task completion) — a live progress bar against a *working* server would have hung waiting for the entire download to finish before ever hearing "upload done." Fixed by closing `upload` inside `headCompletion` too (a response head can only exist once the body finished sending), mirroring `Internals.ClientResponseReceiver.didReceiveHead`'s own redundant `upload.close()` right alongside `didSendRequest`'s.
4. **A test written to dodge `LocalServer`'s known `uploadTask(withStreamedRequest:)` incompatibility (5f, still unresolved — see 7b4) failed instead of passing, and that failure was itself informative.** The attempt: only consume `.upload(_:)` steps from `Internals.AsyncResponse` and stop before ever awaiting `.download(_:)`, on the theory that upload always finishes before a response is expected. Run, not just reasoned about — it still hit the hang, because `Internals.AsyncResponse.Iterator.next()` decides "no more uploads" and *starts draining `head` for the response* inside that same call to `next()`, before ever returning "nothing more" to the caller's loop. There is no `.upload(_:)` step a consumer can stop at that's guaranteed not to trigger that drain. Both combined-overload tests are `withKnownIssue`-wrapped as a result, same root cause and message as `RequestConfigurationURLSessionClientUploadTests` (5f).

**Dependencies:** 7b1.
**Acceptance:** new tests for the combined overload — byte-for-byte upload/download order and completeness, upload progress delivered in increasing order, backpressure, cancellation mid-stream — matching 5f/5g's own bar, still exercising `Internals.URLSessionClient` directly (no `RawTask` involved yet). A cache-tee test under `.urlSession` mirroring 5d's cookie test's own discipline: temporarily break the tee, confirm the test fails, restore it, confirm it passes — proving the test is meaningful, not tautological. Landed as `InternalsURLSessionClientSessionTaskTests` (`RequestDLInternalsTests`): a non-streaming round trip asserts exact chunk-length boundaries and full-body reassembly (mirroring 5g's own stronger-than-minimum bar); a cache-tee test asserts the tee stream and the caller-facing stream assemble to identical bytes, verified non-tautological the same way as 5d (temporarily removed the `cacheStream` call, confirmed the test failed, restored it); a cancellation test cancels mid-download and polls `isRunning` back to `false`; the two streamed-upload-and-download tests are confirmed `withKnownIssue` per finding 4 above. Full suite (1218 tests, 7 known issues — the 5 pre-existing plus these 2) passes; `swift format lint --recursive --strict` clean on every file this sub-phase touched.

### 7b3. Wire it into `RawTask` — real end-to-end traffic

**Goal:** close 7a's own documented caveat for real: `RawTask.result()` dispatches through `Internals.ClientManager.resolvedClient(provider:sessionConfiguration:)` (Phase 6) instead of the NIO-only `client(provider:sessionConfiguration:)`, so `preferredExecutor`/`requiredExecutor` actually decide which backend a real request runs over.

- [x] `RawTask.result()` resolves the 7b1-protocol-conforming client via `resolvedClient(...)`, branching between the 7b1 (NIO) and 7b2 (`URLSessionClient`) conformances. Landed as `Internals.Session.resolvedClient()` (`Sources/RequestDLInternals/.../Session/Internals.Session.swift`) — a new method alongside the existing NIO-only `client()`, not a replacement for it (that method still has real callers: `SessionExecutionTests`, `LocalServerConcurrencyTests`, `InternalsClientResponseReceiverTests`, all deliberately exercising NIO directly). Returns `Internals.ClientManager.Client` (the enum), not `any RequestExecutingClient` -- that protocol and both its conformances live in `RequestDL`, which depends on `RequestDLInternals`, not the reverse, so `Internals.Session` (in `RequestDLInternals`) cannot return the existential itself. `RawTask.result()` is what unwraps the enum into `any RequestExecutingClient` via a `switch`.
- [x] `Internals.CacheControl` runs unmodified against whichever conformance it's handed (this is exactly what 7b1's refactor was for). Confirmed, not just assumed: no changes were needed there, and the full suite (including every `Internals.CacheControl` test) passes unmodified.
- [x] Revisit `enableNetworkFramework(_:)`'s deprecation question (deferred in 7a) now that there's a real answer to give instead of a forward reference. **Answer at the time 7b3 landed: still don't deprecate it, and the reason had narrowed** -- 7a's premise ("`enableNetworkFramework` is still the only modifier that gates NIOTransportServices in the live pipeline") survived 7b3 intact, because 7b3's unification only ever covered the `.urlSession`-vs-not decision, not the NIOTransportServices-vs-plain-NIO choice within the "not `.urlSession`" branch. **Superseded the same day by Phase 7b3.5, below**, which closes that second gap -- `enableNetworkFramework(_:)` is now actually deprecated.

### 7b3.5. Unify the NIOTransportServices-vs-plain-NIO decision too

**Goal:** close the gap 7b3's own acceptance note left open -- `Internals.ClientManager.resolvedClient(...)`'s `.nio` fallback branch called the unchanged `client(provider:sessionConfiguration:)` unmodified, which decides NIOTransportServices-vs-plain-NIO purely from `enableNetworkFramework`/`isCompatibleWithNetworkFramework`, never from `resolveExecutor()`'s own (already-correct, since Phase 3) answer for that exact choice. `preferredExecutor(.nioTransportServices)`/`requiredExecutor(.nioTransportServices)` validated and threw correctly but had zero effect on which event loop group a real request actually got -- the same *shape* of gap 7b3 closed for `.urlSession`, just on the other axis. Raised by the user directly asking "isn't `enableNetworkFramework` deprecatable now?" immediately after 7b3 landed; investigating that question surfaced the gap.

- [x] `Internals.ClientManager.resolvedClient(...)`'s fallback branch now computes `isCompatibleWithNetworkFramework` from `resolveExecutor() == .nioTransportServices`, not from the flag. Landed via a shared `_nioClient(provider:sessionConfiguration:isCompatibleWithNetworkFramework:)` private helper both `client(provider:sessionConfiguration:)` (flag-based, unchanged, still has real NIO-specific test callers: `SessionExecutionTests`, `LocalServerConcurrencyTests`, `InternalsClientResponseReceiverTests`) and `resolvedClient(...)`'s fallback (`resolveExecutor()`-based, new) both call -- the cache-keying mechanism (`SessionProvider.uniqueIdentifier(with:)`'s `"NTW."` prefix) already read an explicit `Bool` parameter, not `enableNetworkFramework` directly, so no change was needed there to keep the two paths from colliding.
- [x] **A real, significant backward-compatibility issue found and fixed before this could ship**: unifying the decision this way means `resolveExecutor()`'s default priority (`.urlSession` first, unconditionally, on Darwin) now applies to the NIOTransportServices axis too -- so a caller who only ever calls `enableNetworkFramework(true)` (already-released public API, `Session.enableNetworkFramework(_:)`, predating `preferredExecutor` entirely) would silently start getting `.urlSession` instead of NIOTransportServices on any config compatible with both, the moment this ships, without touching a line of their own code. Confirmed empirically before deciding how to handle it, not assumed. Raised to the user directly as a judgment call (three options: implicit-preference shim, only-unify-when-explicit, or accept-and-document) -- **decision: `enableNetworkFramework(true)` is now treated as an implicit `preferredExecutor(.nioTransportServices)`** whenever nothing else already set an explicit preference (`resolveExecutor()`'s new `effectivePreferredExecutor = preferredExecutor ?? (enableNetworkFramework ? .nioTransportServices : nil)`), so the flag's historical behavior is preserved byte for byte for existing callers; an explicit `preferredExecutor` (any case, including `.urlSession`) still overrides it.
- [x] `enableNetworkFramework(_:)` is now formally deprecated (`@available(*, deprecated, message: ...)`), pointing at `.preferredExecutor(.nioTransportServices)` -- safe now that the two are functionally equivalent for every real (production) call path, unlike when 7a/7b3 each considered and declined this.

**Dependencies:** 7b3.
**Acceptance:** `InternalsClientManagerExecutorTests` gained two tests proving the unification directly by inspecting `Internals.Client.eventLoopGroup`'s concrete type (`NIOTSEventLoopGroup` vs. not) rather than only the `Internals.ClientManager.Client` enum case, since both `.nio` cases (plain and NIOTransportServices-backed) share that one case -- `preferredExecutor(.nioTransportServices)` alone (flag never touched) actually gets a `NIOTSEventLoopGroup`-backed client, and `requiredExecutor(.nio)` alone never does even on an otherwise-fully-compatible config. `InternalsSessionConfigurationExecutorTests` gained a new "resolveExecutor() with enableNetworkFramework" section (three tests) covering the backward-compat shim specifically: the flag alone resolves to `.nioTransportServices`, an explicit `preferredExecutor` overrides it, and a NIOTransportServices-incompatible field still falls through past it correctly. Full suite (832 + 411 tests) passes.

**A real, unrelated bug found and fixed along the way, not spuriously bundled in:** `resolveExecutor()`'s own doc comment already claimed `requiredExecutor` "lets a caller override it," but the implementation only ever consulted `preferredExecutor` -- `requiredExecutor(.nio)` validated (via `requireExecutor(_:)`, called separately by `RawTask.result()`) without ever actually being the executor a real request dispatched over, since `resolveExecutor()` would still resolve a `.urlSession`-compatible config to `.urlSession` regardless of the pin. Invisible before 7b3, since nothing dispatched for real either way; caught by `DataTaskTests.dataTask_whenCAEnabled()` (a client-certificate mTLS test pinned to `.requiredExecutor(.nio)` specifically to stay off `.urlSession`'s unconditional Keychain-Sharing gap) still hitting that gap after the pin. Fixed: `resolveExecutor()` now returns `requiredExecutor` unconditionally, before any compatibility check, when it's set -- trusting that `requireExecutor(_:)` already validated it (or accepting the caller's own risk if it didn't, same contract `Internals.ClientManager.resolvedClient`'s callers already have). New unit coverage: `InternalsSessionConfigurationExecutorTests`'s "resolveExecutor() with requiredExecutor" section (`RequestDLInternalsTests`).

**A second, larger latent gap found and fixed, also exposed only once real dispatch was possible:** `UploadTaskTests.uploadTask()` and three other tests (two in `ModifiersProgressTests`, one in `AsyncResponseCollectTests`) started failing with `NSURLErrorDataLengthExceedsMaximum` (-1103, "resource exceeds maximum size") the moment `RawTask` could reach `.urlSession` for real. Root-caused empirically (a standalone repro against a real external server, not LocalServer- or beta-OS-specific): **a GET request carrying a body fails outright under URLSession/CFNetwork** -- confirmed with both `uploadTask(with:from:)`/`.upload(for:from:)` and plain `dataTask`/`.data(for:)` with `httpBody` set, same error either way. AsyncHTTPClient (the NIO backend) tolerates this silently; URLSession does not. None of the four failing tests set an explicit `RequestMethod`, so their `Payload(data:)`-bearing requests fell through to the `"GET"` default that `buildURLRequestWithoutBody()`/`build(eventLoop:)` both apply at the very last step -- a pre-existing latent bug in the test suite (and, more precisely, in `Payload`/`Form`'s own method defaulting) that Phase 7b3's real dispatch is simply the first thing to actually expose. Fixed at the source, not by patching the tests around it: `PayloadNode.setBodyWithBuffer` and `FormNode.make` (`Sources/RequestDL/Properties/Sources/Payloads/...`) now default `RequestConfiguration.method` to `"POST"` when attaching a raw-buffer body and nothing has already set a method -- never overriding an explicit `RequestMethod` (whichever property's node runs first wins, and `RequestMethod`'s own node still assigns unconditionally), and never touching the separate `.urlEncoded`/query-string branch (`Payload`'s own "no method means send as a query" behavior for key-value payloads, unaffected). Audited for blast radius before landing: no test in the suite asserted on `.method` being `nil`/`"GET"` alongside a body, so this changed zero test outcomes from pass to fail on its own. Two knock-on updates were still needed and are real, not incidental: `MockedTaskTests`'s two tests asserting on the full mirrored header set now include `rdl-request-method: POST` (a debug/inspection header `PropertyMockedTask` already added conditionally on `method != nil`, now populated earlier and more accurately); and `ModifiersProgressTests.progress_whenCompleteProgress_shouldBeValid()` -- which asserts *exact* per-`payloadChunkSize(64)` upload progress increments, a genuine NIO/streaming-body-specific guarantee documented as executor-specific since Phase 5f -- is now pinned to `.requiredExecutor(.nio)` (its download-side exact-chunk-boundary assertion stays unpinned, since Phase 5g already confirmed that one holds on `.urlSession` too).

**Acceptance, met:** `RawTaskExecutorDispatchTests` (new, `RequestDLTests`) proves real dispatch through the *public* `DataTask` API, one layer up from Phase 6's `InternalsClientManagerExecutorTests` (which used a hand-built `Internals.ClientManager`/`SessionProvider`, bypassing the `Property`/`Resolve` pipeline entirely): a real `DataTask` call is made through an ordinary resolved `Property` tree, then the *identical* tree is resolved again and asked what `Internals.ClientManager.shared` -- the same pool the real call used -- actually cached for that exact provider, confirming `.urlSession` (no executor preference set) and `.nio` (`.requiredExecutor(.nio)` set, proving the bug fix above) respectively. Parity: `UploadTaskTests`/`DownloadTaskTests` each gained a `.requiredExecutor(.urlSession)`-pinned variant of their happy-path test (Darwin-only -- `.urlSession` isn't a real executor anywhere else), alongside their existing default-preference test, which -- now that `.urlSession` is Darwin's default preference when compatible (Phase 3) -- was *already* exercising `.urlSession` implicitly the whole time this phase was being scoped. Full suite (832 + 406 tests) passes, including three runs in a row to check for flakiness under real concurrent dispatch through the newly-live shared `Internals.ClientManager.shared` pool -- none observed.

### 7b4. Testing gaps this uncovers

**Goal:** close two gaps identified while scoping 7b1–7b3, rather than shipping 7b3 with them as unexamined risk.

- [x] **Streamed-upload integration testing.** Originally scoped (see the struck-through framing below, kept for history rather than deleted) as "teach `LocalServer` to answer the draft's negotiation correctly, or accept it as a known issue" — a same-day, much deeper investigation (2026-08-24) overturned the premise that framing was built on. **Full write-up: `INPUT_STREAM_ANALISYS.md` (repo root).** Summary of what changed:

  - The Phase 5f claim this item originally relied on — "a real server (verified against `httpbin.org`) completes normally" — turned out to be true only for a bare `InputStream(data:)` probe that never actually exercised `Internals.URLSessionUploadStream`. Re-run with the real bridge directly against `https://httpbin.org/post`: **it also hangs and times out**, exactly like `LocalServer`. There is no "real server vs. `LocalServer`" distinction to lean on; that framing is retired.
  - The actual variable, isolated across four different servers (`LocalServer`; a from-scratch Python HTTP/1.1 server with correct chunked-decoding; two independent HTTP/2 servers — one built with `swift-nio-http2`, one with Python's `h2` library and zero SwiftNIO involvement; and `httpbin.org` itself) and three independently-written `InputStream` subclasses (RequestDL's real `Internals.URLSessionUploadStream`; an earlier broken hand-rolled one; a deliberately minimal, textbook-correct one with no concurrency at all): **`uploadTask(withStreamedRequest:)`'s resumable-uploads-draft negotiation only completes when the supplied `InputStream` is Foundation's own concrete `InputStream(data:)` class. Any custom subclass — regardless of how correctly it implements the documented `Stream`/`InputStream` contract — hangs identically.** Not HTTP/1.1 vs. HTTP/2 (ruled out with two independent HTTP/2 servers). Not loopback vs. LAN vs. internet (ruled out binding a server to the machine's real LAN IP, and separately against `httpbin.org`). Not a missing `StreamDelegate` notification (tried adding `.endEncountered`/`.hasBytesAvailable`/`.errorOccurred` calls to `Internals.URLSessionUploadStream`, confirmed it doesn't help, reverted — `read(_:maxLength:)` is only ever called *once* in the failing case, so a fix that only fires from inside a second `read()` call can never be reached).
  - Given this, "teach `LocalServer` to speak the draft" (the original plan for this checklist item) is very likely not viable on its own — a from-scratch, protocol-correct HTTP/2 server (Python's `h2`) hit the identical wall, so the gap is client-side (CFNetwork), not something any server-side fix closes.
  - `INPUT_STREAM_ANALISYS.md` hands this to a dedicated, standalone SPM package (RequestDL-free, SwiftNIO-free — deliberately, so nothing about this repo's own dependencies can be blamed) to keep investigating further callback-level hypotheses (real run-loop event delivery through `schedule(in:forMode:)`, a `CFGetTypeID()`-level concrete-type check) — all exhausted without finding the actual mechanism (`copyProperty`/`setProperty`, `getBuffer`, and a genuine `CFReadStream` built from raw callbacks were each tried and ruled out too; see the standalone doc's §4.8–§4.10). Root cause stays unconfirmed. What *did* resolve this item, and is now shipped: the workaround, **file-backed upload (`uploadTask(with:fromFile:)`)**, sidestepping the custom-`InputStream` question entirely by draining to a temp file instead of streaming through a subclass.
  - **Done (2026-08-25):** `Internals.URLSessionUploadStream` (the `InputStream` subclass) is deleted; `Internals.URLSessionUploadFile` (`Sources/RequestDLInternals/.../URLSession Client/Upload/Internals.URLSessionUploadFile.swift`) replaces it, in both `Internals.URLSessionClient.execute(request:streaming:delegate:onUploadProgress:)` (Phase 5f) and the combined upload+download `executeSessionTask` (Phase 7b2). Neither goes through `InputStream`/`needNewBodyStream` any more — `TaskDelegate` no longer implements `needNewBodyStream` at all, since neither of the two upload shapes below needs it (a retry/redirect that resends the body gets a fresh read from wherever the bytes already are, not a callback this delegate has to answer). All four `withKnownIssue` wrappers this bug caused now genuinely pass: `RequestConfigurationURLSessionClientUploadTests`'s two tests (`RequestDLTests`) and `InternalsURLSessionClientSessionTaskTests`'s two `sessionTask_whenStreamingUpload...` tests (`RequestDLInternalsTests`). New unit coverage for the replacement bridge: `InternalsURLSessionUploadFileTests` (`RequestDLInternalsTests`), replacing the deleted `InternalsURLSessionUploadStreamTests`. Full suite re-run clean (825 + 396 tests across both targets) — no regressions.

    **Refined same day, after initial review:** the first cut of this fix drained every streamed body to a temporary file unconditionally, same as the old bridge minus the `InputStream` — correctness-wise fine, but wasteful disk I/O for the common case (a JSON payload, a small `Payload(data:)` upload) that was already sitting in memory as `Data` a moment earlier. `Internals.URLSessionUploadFile.write(body:inMemoryThreshold:)` now drains up to `inMemoryThreshold` (8 MiB, a `static let` on the type) into memory first; if the body finishes within that, it returns `.data(Data)` and the caller uploads via `uploadTask(with:from:)` -- no file, no disk I/O at all. Only a body that turns out to exceed the threshold spills what's already buffered plus the remainder to a temporary file and returns `.file(...)`, same as before. This is a `Materialized` enum (`.data`/`.file`), not a boolean -- `Internals.URLSessionClient` switches on it at each of its two upload-driving call sites to pick `uploadTask(with:from:)` vs. `uploadTask(with:fromFile:)` vs. (no body at all) `dataTask(with:)`. `InternalsURLSessionUploadFileTests` covers both branches directly (a small payload takes `.data`, a payload driven past a deliberately tiny test-only threshold takes `.file`), including each branch's own error/cleanup path. Both `RequestConfigurationURLSessionClientUploadTests` payloads (256 KiB, 128 KiB) sit under the real 8 MiB default, so they now exercise the in-memory branch specifically -- see that file's updated type doc comment.

    **A related, larger optimization, first deferred then done the same day on request:** when the body is backed by a single file already on disk (`Payload(url:)`), the code above still drained it through the generic `AsyncSequence` path and re-copied it into a *new* temporary file before uploading -- doubling the I/O for exactly the large-file-upload case where it matters most. This is now avoided: `Internals.Buffer.wholeFileURL` (`Sources/RequestDLInternals/.../Buffers/Buffer/Internals.Buffer.swift`, `Stream == Internals.FileStreamBuffer` only) answers with the file's own `URL` when this cursor hasn't read anything yet (`readerIndex == .zero`) and the file isn't one this package created for scratch use (`!storage.url.isTemporary` -- `Internals.FileBufferURL.isTemporary` bumped from `private` to `package` to make this checkable at all); `Internals.BodySequence.wholeFileURL` answers it only when the whole body is exactly one such buffer (not multipart, not a `DataBuffer`); `RequestBody.wholeFileURL` (internal, `RequestDL`) forwards that. `Internals.URLSessionUploadFile.Materialized` gained a third case, `.existingFile(URL)` -- upload via `uploadTask(with:fromFile:)` same as `.file`, but never removed afterward, since it isn't this package's file. Both `execute(request:streaming:...)` overloads on `Internals.URLSessionClient` gained an `existingUploadFile: URL? = nil` parameter that skips `Internals.URLSessionUploadFile.write(body:)` entirely when set; `Internals.URLSessionClient+RequestExecutingClient.swift` passes `body.wholeFileURL` through unconditionally (`nil` when it doesn't qualify, which is a no-op). New coverage across all three layers: `InternalsFileBufferTests`/`InternalsBodySequenceTests` (buffer/sequence-level `wholeFileURL` gating -- unread vs. already-read, temporary vs. not, single vs. multipart), `RequestBodyBuildingTests` (the `RequestBody` forward), and `urlSessionClient_whenStreamingUploadFromExistingFile_deliversWholeBodyIntact` (`RequestConfigurationURLSessionClientUploadTests`, a real `Payload(url:)` round trip over `.urlSession` confirming the shortcut still delivers exact bytes, not just that the URL forwards correctly). Full suite re-run clean (828 + 403 tests) — no regressions.

    One real, documented behavior change from the old (broken) approach remains for the file-spillover branch specifically: a genuinely large body is written to disk before the upload task starts, rather than pulled a slice at a time as `URLSession` sends it — unavoidable given `uploadTask(with:fromFile:)` needs a complete file up front. `RequestConfiguration.buildURLRequest()`/`buildURLRequestWithoutBody()`'s doc comments were updated accordingly rather than left claiming the old "does not buffer `body` at all."

    The underlying CFNetwork bug itself is still not root-caused or reported to Apple — see `INPUT_STREAM_ANALISYS.md` §9's updated "Done state" for why closing this checklist item via the workaround doesn't mean that investigation is finished, just that it stopped being a blocker here.
  - ~~A real server (verified in Phase 5f against `httpbin.org`) ignores headers it doesn't recognize and completes normally; `LocalServer` — the test suite's own minimal hand-rolled NIOHTTP1 server — answers with a perfectly ordinary, correctly framed 200, but CFNetwork's draft-negotiation state machine never recognizes that response as settling the exchange it silently started, and the task hangs until timeout. Options for 7b4: teach `LocalServer` to answer the draft's negotiation correctly (scope unknown until investigated), or make a deliberate, documented call to test this one path against a real external server / accept it as a known issue the way 5f did.~~ *(superseded by the summary above — kept struck through rather than deleted so the corrected framing is traceable against what this item used to say.)*
- [ ] **Cancellation, validated for real.** `TaskSeed` (the type that actually cancels an in-flight request) is already transport-agnostic — a `.urlSession`-backed cancel closure is expected to be simpler than `Internals.UnsafeTask`'s NIO/event-loop-affinity dance, not harder. What hasn't been exercised is cancelling a `DataTask`/`DownloadTask` mid-flight while it's genuinely running over `.urlSession` inside the real `RawTask` pipeline (via `Session.requiredExecutor(.urlSession)`) — confirming the underlying `URLSessionTask` is actually cancelled, the throttle slot releases, and nothing leaks, with the same scrutiny `Internals.UnsafeTask`'s own doc comments already hold the NIO path to.

**Dependencies:** 7b3. The streamed-upload item's dependency on `INPUT_STREAM_ANALISYS.md`'s standalone investigation is resolved: it landed on the workaround outcome (file-backed upload), not root cause or "neither" — see that document's §9.
**Acceptance:** Streamed-upload item: met — the four previously-`withKnownIssue` tests genuinely pass; see the "Done" note above. Cancellation item: still TBD.

---

## Phase 8 — Runtime Keychain error UX (can start once Phase 5e lands)

- [ ] Confirm the wrapped error from 5e actually reads well end to end — i.e., someone who hits it without having read `HOW_TO_USE_CERTIFICATE_URLSESSION.md` can self-diagnose "enable Keychain Sharing" from the error text alone.
- [ ] Cross-link the error's documentation comment to `HOW_TO_USE_CERTIFICATE_URLSESSION.md` (or fold that doc's content into a DocC article once the feature ships — it was written as a standalone Markdown file specifically because the feature wasn't shippable yet).

**Dependencies:** Phase 5e.

---

## Phase 9 — Test parity strategy

Not a phase with its own deliverable — a constraint that applies across Phase 5's sub-phases: **every behavioral test that exists for the NIO backend and has a bucket A/B/C equivalent should be parameterized or duplicated to also run against `.urlSession`**, using `requireExecutor(.urlSession)` (Phase 3) to force it deterministically rather than relying on `resolveExecutor()`'s heuristics in tests. Bucket D items (§4.4) don't need this — there's nothing to test on the URLSession side for those, they're fallback-only by design.

---

## Phase 10 — Follow-up, non-blocking

Tracked so they don't get lost, not required for the executor to ship:

- [ ] PKCS#8 and EC key support in the promoted `RawBytesIdentityBuilder` (RSA/PKCS#1 only today).
- [ ] tvOS/watchOS physical validation of the Keychain round-trip (expected to match iOS, unconfirmed).
- [ ] Non-sandboxed macOS validation — genuinely unresolved, hit a *different* Keychain identity-pairing issue in earlier testing, unrelated to entitlements. Don't ship macOS mTLS-via-URLSession support without dedicated testing there.
- [ ] SOCKS proxy support, if 5c determines it's viable after all.
- [ ] Investigate a redirect-cycle test crash (`InternalsURLSessionClientRedirectTests.execute_whenRedirectRevisitsURL_throwsRedirectCycleDetectedError`/`execute_whenRedirectAllowsCycles_followsBackToAVisitedURL`) found while cross-platform-validating 5c: both pass cleanly alone or alongside unrelated suites on every platform including iOS, but crash the test process on iOS Simulator specifically when run concurrently with several other `LocalServer`-backed suites at once (swift-testing's default parallelism). Reproducible; looks like Simulator-specific resource contention (ports/threads) across multiple concurrent `MultiThreadedEventLoopGroup`s rather than a defect in the redirect logic itself (Phase 5b), since serializing or isolating the suite makes it disappear. Not investigated further here — out of scope for 5c.

---

## Topics for future exploration

Not a phase, not scoped, nothing here is required for anything above to ship. Tracked so a real
direction doesn't get lost the next time someone asks "can this do X" and the answer turns out to
be "not with what's built."

### Background `URLSession` (`URLSessionConfiguration.background(withIdentifier:)`) + `AppDelegate`

Background transfers are cited in `URLSESSION_REPORT.md` (§6.3) as one of the original
motivations for wanting a URLSession executor at all — but nothing in Phases 1–7 actually delivers
it, and it doesn't fall out of Phase 6 (wiring) or Phase 7 (public API) for free. The design built
across 5a–5g is incompatible with background sessions in specific, structural ways, not just
missing a flag:

- **Streamed upload (5f) is a disallowed API shape for background sessions.** Apple requires
  background uploads to come from a file (`uploadTask(with:fromFile:)`); `uploadTask(withStreamedRequest:)`
  — what `Internals.URLSessionUploadStream` bridges into — isn't a valid call at all in a
  background session.
- **Streamed download (5g) works against the grain of how background sessions are meant to be
  used.** A background download is expected to be a `URLSessionDownloadTask` writing straight to a
  temp file, delivered via `didFinishDownloadingTo`, not accumulated in-process through
  `didReceive data:` the way `Internals.DownloadBuffer` is fed today.
- **Per-task delegates (`task.delegate = ...`) don't survive process relaunch.** Every `execute`
  overload built so far relies on this pattern (since 5a) precisely because it's the ergonomic
  match for `async`/`await`. But the entire point of a background session is continuing after the
  app is suspended or killed — when `application(_:handleEventsForBackgroundURLSession:completionHandler:)`
  relaunches the app, there is no surviving `URLSessionTask` object to have set a delegate on in
  the first place. Reattaching only works through the *session's* own delegate, recreated with the
  same identifier, not a per-task one.
- **`Internals.URLSessionClient`'s session lifecycle doesn't match.** It creates one `URLSession`
  inline per client instance today. A background session needs a stable identifier and a lifecycle
  that can outlive (and be recreated independently of) whatever `Internals.URLSessionClient`
  instance originally created it.

None of this means it's not doable — it means it's a materially different code path from 5f/5g's
streaming bridges, not an extension of them: a session-level (not per-task) delegate design,
file-based upload/download instead of streams, and a way for a host app to hand this package its
`AppDelegate`'s background completion handler and get reconnected to in-flight tasks after a
relaunch. Worth a dedicated design pass of its own — probably its own report section, the way
`URLSESSION_REPORT.md` covers the executor split — rather than being squeezed into the existing
phase numbering.

---

## References

- [URLSESSION_REPORT.md](URLSESSION_REPORT.md) — full compatibility mapping and design rationale every phase above cites.
- [HOW_TO_USE_CERTIFICATE_URLSESSION.md](HOW_TO_USE_CERTIFICATE_URLSESSION.md) — consumer-facing Keychain Sharing walkthrough (Phase 5e/8).
- [request-dl-nio#287](https://github.com/request-dl/request-dl-nio/discussions/287) — closed, mTLS feasibility discussion.
- [#289](https://github.com/request-dl/request-dl-nio/pull/289) — merged, the two prerequisite fixes Phase 2 builds on.
- `Tests/RequestDLTests/URLSession Executor Spike/` — the validated spike Phase 5e promoted into production code (`Internals.RawBytesIdentityBuilder`, `Internals.URLSessionIdentityPolicy`, `Internals.URLSessionClient.TLSDelegate`); the directory itself is deleted, superseded by `RequestConfigurationURLSessionClientMTLSTests`.
