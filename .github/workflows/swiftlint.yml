name: Swiftlint

on:
  pull_request:
    paths:
      - '.swiftlint.yml'
      - 'Sources/**'
      - 'Tests/**'
      - '.github/workflows/swiftlint.yml'

jobs:
  swiftlint:
    uses: request-dl/.github/.github/workflows/swiftlint.yml@main